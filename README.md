# Food Diary

A minimal React + Vite starter for a food recipe website.

## Setup

1. Install dependencies: `npm install` or `yarn`
2. Start dev server: `npm run dev`

## Structure
- `src/` - React source files
- `data/recipes.json` - sample data
- `assets/` - images and media

Feel free to edit and extend components.
