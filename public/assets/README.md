Place recipe images in this folder (e.g., `recipe1.jpg`), and reference them from data/recipes.json using `/assets/your-image.jpg`.

For now the sample data references `/assets/placeholder.png`. Add an image named `placeholder.png` here if you want a visible thumbnail.
